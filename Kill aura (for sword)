local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer

-- Очистка старого интерфейса
if CoreGui:FindFirstChild("sKillAura_iOS_V2") then CoreGui.sKillAura_iOS_V2:Destroy() end

local sg = Instance.new("ScreenGui", CoreGui)
sg.Name = "sKillAura_iOS_V2"
sg.IgnoreGuiInset = true
sg.ResetOnSpawn = false

-- // КНОПКА ОТКРЫТИЯ (Apple Style)
local openBtn = Instance.new("TextButton", sg)
openBtn.Size = UDim2.new(0, 60, 0, 60)
openBtn.Position = UDim2.new(0, 20, 0.5, -30)
openBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
openBtn.BackgroundTransparency = 0.5
openBtn.Text = ""
openBtn.Draggable = true
openBtn.Active = true

local bCorner = Instance.new("UICorner", openBtn)
bCorner.CornerRadius = UDim.new(0, 18)

local bStroke = Instance.new("UIStroke", openBtn)
bStroke.Color = Color3.fromRGB(255, 255, 255)
bStroke.Thickness = 2
bStroke.Transparency = 0.7

local inner = Instance.new("Frame", openBtn)
inner.Size = UDim2.new(0, 26, 0, 26)
inner.Position = UDim2.new(0.5, -13, 0.5, -13)
inner.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
inner.BackgroundTransparency = 0.3
inner.Active = false
Instance.new("UICorner", inner).CornerRadius = UDim.new(1, 0)

-- // ГЛАВНОЕ МЕНЮ
local main = Instance.new("CanvasGroup", sg) -- Используем CanvasGroup для плавного исчезновения всей группы
main.Size = UDim2.new(0, 240, 0, 0)
main.Position = UDim2.new(0.5, -120, 0.5, 0)
main.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
main.BackgroundTransparency = 0.2
main.ClipsDescendants = true
main.Visible = false
main.GroupTransparency = 1 -- Для анимации появления

Instance.new("UICorner", main).CornerRadius = UDim.new(0, 28)
local mStroke = Instance.new("UIStroke", main)
mStroke.Thickness = 1.5
mStroke.Color = Color3.fromRGB(255, 255, 255)
mStroke.Transparency = 0.8

-- Контейнер для отступов
local container = Instance.new("Frame", main)
container.Size = UDim2.new(1, -40, 1, -40)
container.Position = UDim2.new(0, 20, 0, 20)
container.BackgroundTransparency = 1

local layout = Instance.new("UIListLayout", container)
layout.Padding = UDim.new(0, 12)
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local title = Instance.new("TextLabel", container)
title.Size = UDim2.new(1, 0, 0, 30)
title.BackgroundTransparency = 1
title.Text = "SKILL AURA"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 16
title.TextTransparency = 0.2

-- Кнопка переключения
local toggle = Instance.new("TextButton", container)
toggle.Size = UDim2.new(1, 0, 0, 48)
toggle.BackgroundColor3 = Color3.fromRGB(255, 59, 48)
toggle.Text = "Aura: Disabled"
toggle.TextColor3 = Color3.new(1, 1, 1)
toggle.Font = Enum.Font.GothamMedium
toggle.TextSize = 14
toggle.AutoButtonColor = false
Instance.new("UICorner", toggle).CornerRadius = UDim.new(0, 14)

-- Поле ввода дистанции
local boxFrame = Instance.new("Frame", container)
boxFrame.Size = UDim2.new(1, 0, 0, 48)
boxFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
boxFrame.BackgroundTransparency = 0.92
Instance.new("UICorner", boxFrame).CornerRadius = UDim.new(0, 14)

local box = Instance.new("TextBox", boxFrame)
box.Size = UDim2.new(1, -20, 1, 0)
box.Position = UDim2.new(0, 10, 0, 0)
box.BackgroundTransparency = 1
box.Text = "Range: 5"
box.PlaceholderText = "Set Range..."
box.TextColor3 = Color3.new(1, 1, 1)
box.Font = Enum.Font.Gotham
box.TextSize = 14

-- // АНИМАЦИИ ОТКРЫТИЯ
local isOpen = false
openBtn.MouseButton1Click:Connect(function()
	isOpen = not isOpen
	if isOpen then
		main.Visible = true
		TweenService:Create(main, TweenInfo.new(0.5, Enum.EasingStyle.Exponential, Enum.EasingDirection.Out), {
			Size = UDim2.new(0, 240, 0, 190),
			Position = UDim2.new(0.5, -120, 0.5, -95),
			GroupTransparency = 0
		}):Play()
	else
		TweenService:Create(main, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
			Size = UDim2.new(0, 240, 0, 0),
			Position = UDim2.new(0.5, -120, 0.5, 0),
			GroupTransparency = 1
		}):Play()
		task.wait(0.3)
		if not isOpen then main.Visible = false end
	end
end)

-- // ЛОГИКА АУРЫ (Твоя оригинальная, но с визуальными улучшениями)
local active = false
local range = 5
local visualSphere = nil
local highlights = {}

toggle.MouseButton1Click:Connect(function()
	active = not active
	local targetCol = active and Color3.fromRGB(52, 199, 89) or Color3.fromRGB(255, 59, 48)
	local targetText = active and "Aura: Enabled" or "Aura: Disabled"
	
	TweenService:Create(toggle, TweenInfo.new(0.3, Enum.EasingStyle.Quint), {BackgroundColor3 = targetCol}):Play()
	toggle.Text = targetText

	if not active then
		if visualSphere then visualSphere:Destroy() visualSphere = nil end
		for _, h in pairs(highlights) do h:Destroy() end
		highlights = {}
	end
end)

box.FocusLost:Connect(function()
	local val = tonumber(box.Text:match("%d+%.?%d*")) or 5
	range = val
	box.Text = "Range: " .. tostring(range)
end)

-- Основной цикл (без изменений в логике урона)
local hue = 0
RunService.Heartbeat:Connect(function(dt)
	if not active then return end
	hue = (hue + dt * 0.4) % 1
	local color = Color3.fromHSV(hue, 0.7, 1)

	local char = player.Character
	local hrp = char and char:FindFirstChild("HumanoidRootPart")
	local tool = char and char:FindFirstChildOfClass("Tool")
	local handle = tool and (tool:FindFirstChild("Handle") or tool:FindFirstChildOfClass("Part") or tool:FindFirstChildOfClass("MeshPart"))

	if hrp and handle then
		if not visualSphere then
			visualSphere = Instance.new("Part", workspace)
			visualSphere.Shape = "Ball"
			visualSphere.Material = "ForceField"
			visualSphere.CastShadow = false
			visualSphere.CanCollide = false
			visualSphere.CanTouch = false
			visualSphere.Anchored = true
			visualSphere.Transparency = 0.6
		end
		visualSphere.Size = Vector3.new(range*2, range*2, range*2)
		visualSphere.Color = color
		visualSphere.CFrame = handle.CFrame

		local currentTargets = {}
		for _, v in ipairs(Players:GetPlayers()) do
			if v ~= player and v.Character and (v.Team ~= player.Team or v.Team == nil) then
				local tChar = v.Character
				local tHrp = tChar:FindFirstChild("HumanoidRootPart")
				local tHum = tChar:FindFirstChild("Humanoid")

				if tHrp and tHum and tHum.Health > 0 then
					local dist = (hrp.Position - tHrp.Position).Magnitude
					if dist <= range then
						currentTargets[v.Name] = tChar
						
						if not highlights[v.Name] then
							local h = Instance.new("Highlight", tChar)
							h.FillTransparency = 0.6
							highlights[v.Name] = h
						end
						highlights[v.Name].FillColor = color
						highlights[v.Name].OutlineColor = Color3.new(1,1,1)

						tool:Activate()
						for _, part in ipairs(tChar:GetChildren()) do
							if part:IsA("BasePart") then
								firetouchinterest(handle, part, 0)
								firetouchinterest(handle, part, 1)
							end
						end
					end
				end
			end
		end

		for name, h in pairs(highlights) do
			if not currentTargets[name] then h:Destroy() highlights[name] = nil end
		end
	else
		if visualSphere then visualSphere:Destroy() visualSphere = nil end
	end
end)
