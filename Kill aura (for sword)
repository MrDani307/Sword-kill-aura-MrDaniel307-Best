local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer

if CoreGui:FindFirstChild("sKillAura_Frozen") then CoreGui.sKillAura_Frozen:Destroy() end

local sg = Instance.new("ScreenGui", CoreGui)
sg.Name = "sKillAura_Frozen"
sg.IgnoreGuiInset = true

-- // ПАЛИТРА (Светлый минимализм)
local STYLE = {
    GlassColor = Color3.fromRGB(255, 255, 255),
    MainText = Color3.fromRGB(40, 40, 45),
    AccentOn = Color3.fromRGB(0, 122, 255), -- iOS Blue
    AccentOff = Color3.fromRGB(142, 142, 147), -- iOS Gray
    Stroke = Color3.fromRGB(255, 255, 255),
}

-- // Вспомогательная функция перетаскивания
local function makeDraggable(frame)
    local dragging, dragInput, dragStart, startPos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    frame.InputEnded:Connect(function(input)
        dragging = false
    end)
end

-- // КНОПКА ОТКРЫТИЯ (Frosted Assistive)
local openBtn = Instance.new("TextButton", sg)
openBtn.Size = UDim2.new(0, 55, 0, 55)
openBtn.Position = UDim2.new(0, 25, 0.5, -27)
openBtn.BackgroundColor3 = STYLE.GlassColor
openBtn.BackgroundTransparency = 0.3
openBtn.Text = "S"
openBtn.Font = Enum.Font.GothamBold
openBtn.TextColor3 = STYLE.MainText
openBtn.TextSize = 18
Instance.new("UICorner", openBtn).CornerRadius = UDim.new(1, 0)
local obStroke = Instance.new("UIStroke", openBtn)
obStroke.Color = STYLE.Stroke
obStroke.Thickness = 2
makeDraggable(openBtn)

-- // ГЛАВНОЕ МЕНЮ (Ultra Blur Glass)
local main = Instance.new("CanvasGroup", sg)
main.Size = UDim2.new(0, 270, 0, 210)
main.Position = UDim2.new(0.5, -135, 0.5, -105)
main.BackgroundColor3 = STYLE.GlassColor
main.BackgroundTransparency = 0.45 -- Сильная прозрачность для эффекта размытия
main.Visible = false
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 32)
local mainStroke = Instance.new("UIStroke", main)
mainStroke.Thickness = 2
mainStroke.Color = STYLE.Stroke
mainStroke.Transparency = 0.2
makeDraggable(main)

-- Мягкие блики внутри (создают объем)
for i = 1, 3 do
    local blob = Instance.new("Frame", main)
    blob.Size = UDim2.new(0, 180, 0, 180)
    blob.Position = UDim2.new(math.random()-0.5, 0, math.random()-0.5, 0)
    blob.BackgroundColor3 = Color3.new(1, 1, 1)
    blob.BackgroundTransparency = 0.85
    Instance.new("UICorner", blob).CornerRadius = UDim.new(1, 0)
    task.spawn(function()
        while true do
            local target = UDim2.new(math.random()-0.5, 0, math.random()-0.5, 0)
            TweenService:Create(blob, TweenInfo.new(5, Enum.EasingStyle.Sine), {Position = target}):Play()
            task.wait(5)
        end
    end)
end

-- Контент
local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1, 0, 0, 65)
title.Text = "sKillAura"
title.TextColor3 = STYLE.MainText
title.Font = Enum.Font.GothamBold
title.TextSize = 22
title.BackgroundTransparency = 1

local toggle = Instance.new("TextButton", main)
toggle.Size = UDim2.new(0.85, 0, 0, 50)
toggle.Position = UDim2.new(0.075, 0, 0, 75)
toggle.BackgroundColor3 = STYLE.AccentOff
toggle.Text = "DISABLED"
toggle.TextColor3 = Color3.new(1, 1, 1)
toggle.Font = Enum.Font.GothamMedium
toggle.AutoButtonColor = false
Instance.new("UICorner", toggle).CornerRadius = UDim.new(0, 14)

local box = Instance.new("TextBox", main)
box.Size = UDim2.new(0.85, 0, 0, 50)
box.Position = UDim2.new(0.075, 0, 0, 135)
box.BackgroundColor3 = Color3.new(0, 0, 0)
box.BackgroundTransparency = 0.93
box.Text = "Distance: 5"
box.TextColor3 = STYLE.MainText
box.Font = Enum.Font.Gotham
Instance.new("UICorner", box).CornerRadius = UDim.new(0, 14)

-- // ЛОГИКА
local active = false
local range = 5

openBtn.MouseButton1Click:Connect(function()
    main.Visible = not main.Visible
    if main.Visible then
        main.GroupTransparency = 1
        TweenService:Create(main, TweenInfo.new(0.4, Enum.EasingStyle.Quint), {GroupTransparency = 0}):Play()
    end
end)

toggle.MouseButton1Click:Connect(function()
    active = not active
    local targetColor = active and STYLE.AccentOn or STYLE.AccentOff
    TweenService:Create(toggle, TweenInfo.new(0.3), {BackgroundColor3 = targetColor}):Play()
    toggle.Text = active and "ACTIVE" or "DISABLED"
end)

box.FocusLost:Connect(function()
    range = tonumber(box.Text:match("%d+")) or 5
    box.Text = "Distance: " .. range
end)

-- Основной цикл (Убийство)
RunService.Heartbeat:Connect(function()
    if not active then return end
    
    local char = player.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    local tool = char and char:FindFirstChildOfClass("Tool")
    local handle = tool and (tool:FindFirstChild("Handle") or tool:FindFirstChildOfClass("Part"))

    if hrp and handle then
        for _, v in ipairs(Players:GetPlayers()) do
            if v ~= player and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                local tHrp = v.Character.HumanoidRootPart
                if (hrp.Position - tHrp.Position).Magnitude <= range then
                    tool:Activate()
                    for _, p in ipairs(v.Character:GetChildren()) do
                        if p:IsA("BasePart") then
                            firetouchinterest(handle, p, 0)
                            firetouchinterest(handle, p, 1)
                        end
                    end
                end
            end
        end
    end
end)
